import 'package:flutter/material.dart';

class FollowupContent extends StatelessWidget {
  const FollowupContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('FollowUp Page Content'),
    );
  }
}
